#!/bin/bash
# Medical Protocol Hook: PostToolUse (WebFetch)
# The CDN (medical-protocol.vercel.app) has been decommissioned.
# All content is now served locally via the plugin (${CLAUDE_PLUGIN_ROOT}/reference/).
# This hook is kept as a no-op stub — remove it when hooks config is updated.

exit 0
